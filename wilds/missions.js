const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let all=[];const q=document.getElementById('q'),kind=document.getElementById('kind'),list=document.getElementById('list');function render(){const t=q.value.toLocaleLowerCase(),f=all.filter(x=>(!kind.value||x.model===kind.value)&&(!t||x.name.toLocaleLowerCase().includes(t)));list.innerHTML=f.map(x=>`<article class="card"><div class="card-kicker">${x.model==='missions'?'Misión principal':'Quest'}</div><h2>${esc(x.name)}</h2><a href="${esc(x.source)}" target="_blank" rel="noreferrer">Ver ficha de Kiranico ↗</a></article>`).join('')||'<p class="empty-state">No hay resultados.</p>'}fetch('data/missions.json').then(r=>r.json()).then(v=>{all=v;render()});[q,kind].forEach(e=>e.addEventListener('input',render));
