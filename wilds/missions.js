const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const norm=s=>String(s??'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
const typeInfo={story:{label:'Principal',color:'purple'},assignment:{label:'Encargo',color:'red'},optional:{label:'Opcional',color:'green'},event:{label:'Evento',color:'gray'}};
let all=[],monsterIconManifest={},mode='missions',query='';
const indexEl=document.getElementById('mission-index'),detailEl=document.getElementById('mission-detail'),searchEl=document.getElementById('mission-search');
function typeFor(x){return typeInfo[x.questType]||typeInfo.optional;}
function ticket(x,compact=false){const t=typeFor(x);return `<span class="quest-ticket-icon quest-ticket-icon--${t.color}${compact?' quest-ticket-icon--compact':''}" title="${esc(t.label)}"><span class="quest-ticket-icon__paper"></span><span class="quest-ticket-icon__seam"></span><span class="quest-ticket-icon__fold"></span></span>`;}
function monsterIconFor(name){return monsterIconManifest[name]||'';}
function monsterIcons(x,interactive=false){const names=[...new Set(x.mainMonsters||[])];if(!names.length)return '';return `<span class="quest-monster-icons">${names.map(name=>{const src=monsterIconFor(name);if(!src)return '';const img=`<img class="quest-monster-icon" src="${esc(src)}" alt="${esc(name)}" title="${esc(name)}" loading="lazy">`;return interactive?`<button type="button" class="quest-monster-icon-link" data-monster="${esc(name)}">${img}</button>`:img;}).join('')}</span>`;}
function modeFor(x){return x.model==='missions'?'missions':(x.questType||'optional');}
function filtered(){const term=norm(query);return all.filter(x=>modeFor(x)===mode&&(!term||norm([x.name,x.description,x.boardGroup,...(x.mainMonsters||[])].join(' ')).includes(term)));}
function renderModeButtons(){document.querySelectorAll('[data-mode]').forEach(b=>{const active=b.dataset.mode===mode;b.classList.toggle('active',active);b.setAttribute('aria-selected',String(active));});}
function renderIndex(){detailEl.hidden=true;indexEl.hidden=false;const rows=filtered();if(!rows.length){indexEl.innerHTML='<p class="no-data">No hay misiones que coincidan.</p>';return;}const groups=new Map();for(const x of rows){const group=x.boardGroup||(mode==='missions'?'Missions':typeFor(x).label);if(!groups.has(group))groups.set(group,[]);groups.get(group).push(x);}indexEl.innerHTML=[...groups].map(([group,items])=>`<section class="decorations-slot-group"><h2 class="decorations-slot-heading">${esc(group)}</h2><div class="decorations-grid">${items.map(x=>`<button type="button" class="decoration-card quest-card" data-id="${esc(x.id)}">${ticket(x,true)}<span class="quest-card-copy"><span class="decoration-card-name">${esc(x.title||x.name)}</span><span class="quest-goal quest-goal--compact"><span>${esc(x.description||'—')}</span>${monsterIcons(x)}</span><span class="decoration-card-skill">${esc(typeFor(x).label)}</span></span></button>`).join('')}</div></section>`).join('');indexEl.querySelectorAll('[data-id]').forEach(b=>b.addEventListener('click',()=>showDetail(b.dataset.id)));}
function showDetail(id){const x=all.find(q=>q.id===id);if(!x)return;history.replaceState(null,'',`?q=${encodeURIComponent(id)}`);indexEl.hidden=true;detailEl.hidden=false;const t=typeFor(x);detailEl.innerHTML=`<button type="button" class="decorations-back" id="mission-back">← Volver a misiones</button><div class="decoration-detail-header">${ticket(x)}<h2>${esc(x.title||x.name)}</h2><span class="decoration-detail-slot">${esc(x.boardGroup||'')}</span></div><p class="quest-type-line"><span class="quest-type-badge quest-type-badge--${t.color}">${esc(t.label)}</span></p>${x.description?`<section class="block"><h3>Descripción</h3><p class="gs-material-intro">${esc(x.description)}</p></section>`:''}${(x.mainMonsters||[]).length?`<section class="block"><h3>Monstruos objetivo</h3><ul class="chips">${x.mainMonsters.map(name=>{const src=monsterIconFor(name);return `<li class="chip quest-target-chip">${src?`<button type="button" class="skill-name-link quest-target-link" data-monster="${esc(name)}"><img class="quest-target-icon" src="${esc(src)}" alt="" loading="lazy">${esc(name)}</button>`:esc(name)}</li>`;}).join('')}</ul></section>`:''}<a href="${esc(x.source)}" target="_blank" rel="noreferrer">Ver ficha completa en Kiranico ↗</a>`;detailEl.querySelector('#mission-back').addEventListener('click',()=>{history.replaceState(null,'',location.pathname);renderIndex();});detailEl.querySelectorAll('[data-monster]').forEach(b=>b.addEventListener('click',()=>location.href=`monster.html?m=${encodeURIComponent(b.dataset.monster)}`));window.scrollTo(0,0);}
async function init(){const [rows,icons]=await Promise.all([fetch('data/missions.json').then(r=>r.json()),fetch('data/icon_manifest.json').then(r=>r.json())]);all=rows;monsterIconManifest=icons;searchEl.addEventListener('input',()=>{query=searchEl.value;renderIndex();});document.querySelectorAll('[data-mode]').forEach(b=>b.addEventListener('click',()=>{mode=b.dataset.mode;renderModeButtons();renderIndex();}));renderModeButtons();const id=new URLSearchParams(location.search).get('q');if(id)showDetail(id);else renderIndex();}
init().catch(()=>{indexEl.innerHTML='<p class="no-data">No se pudieron cargar las misiones.</p>';});
