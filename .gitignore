# Reference-only downloads used for research (data-mining tool source /
# zone-chart assets) -- not served by the app, kept locally for future work.
# See PROGRESS.md/DATA_NOTES.md for what they're for.
vectores/mhrice/
vectores/MHRise-Database-main/
# MHFU raw source dumps (mhfu-db + mhfu-asset-db, ~700MB) -- mined via
# Riperino into mhfu/data/*.json + mhfu/data/images/, not served directly.
sources/

# Raw scraped HTML cache for rise/data/scrape_weapon_trees.js -- resumable, not served.
rise/data/weapon_tree_raw/
# Fandom tree raw cache (used by rise/data/build_weapon_tree.js to rebuild the tree)
rise/data/weapon_tree_raw_fandom/

# Playwright MCP browser session output (snapshots/console logs)
.playwright-mcp/

# Weapon icon fallback scraper cache (resumable, not served)
rise/data/weapon_icon_fallback_cache/

# scratch screenshots
weapon_tree_kamura.png

# stray files from a merged external project (bestiario-nemo) â€” not part of the app
node_modules/
package.json
package-lock.json
check_final.py
check_missing*.py
download_all_armor_sets.py
download_missing_armor.js
download_remaining.py
final_download.py
selenium_download_missing.py
